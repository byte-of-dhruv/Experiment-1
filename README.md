Experiment 1
Aim - Introduction to Python

Theory - 
To find the data type of a stored variable we use 'type(variable) with print function'.
There are different data types in python that are string, float, integer, etc.
For user defined inputs in python we use 'input()' function. For the data type of input we define the data type before input function for example 'int(input())'.
For simple and complex mathematical operations we import math module. For example- sin, log, etc.

Algorithm -

For Mathematical Operations:-
1.Assign input values in variable a and b.
2.Display the result. For addition(a+b), subtraction(a-b), multiply(a*b), subtraction(a/b).

For Area of Circle:-
1.import math module.
2.Assign input values in variable r.
3.Display the result. For Radius (math.pi*r**2)

For Comparison:-
1.import math.
2.Assign input values in variable a and b.
3. If a>b, display "a is greater than b". Else print "b is greater than a". 

Conclusion - We learnt to use print, type of variable, add, sum, div, multiply, input user value, radius of circle, num comparison.
